mpu6050:
  # Update output_dir to wherever you store logs on the Pi.
  # "~" expands to the home folder of the pi user.
  output_dir: ~/logs/mpu
  sample_rate_hz: 100
  sensors: [1, 2, 3]
  include_temperature: false

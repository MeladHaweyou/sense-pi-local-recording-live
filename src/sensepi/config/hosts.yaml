# Each Pi uses username/password authentication.
# SSH key authentication is not supported in this version.
pis:
  - name: Pi-06
    host: 192.168.0.6
    user: verwalter
    password: "!66442200"
    base_path: /home/verwalter/sensor
    data_dir: /home/verwalter/logs
    pi_config_path: /home/verwalter/sensor/pi_config.yaml
